# Application dependencies

require:
    - play
    - play -> mongo 1.3
    - com.alibaba -> fastjson 1.2.1:
        transitive: false
    - api -> api

repositories:
    - biz modules:
        type:       local
        artifact:   ${application.path}/../[module]
        contains:
          - api

